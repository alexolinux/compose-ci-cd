# compose-ci-cd
Orchestrated CI/CD for Personal Lab (docker-compose)

.
├── code-server
├── docker-compose.yml
├── jenkins
│   └── Dockerfile
├── LICENSE
├── .env
└── README.md

`.env` Example

```shell
WORKSPACE="${HOME}/workspace"
```

